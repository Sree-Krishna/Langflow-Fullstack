langflow
Flask              # Flask web framework for creating the API
transformers      # Hugging Face Transformers library for LLMs
torch              # PyTorch, required for running models
torchvision        
gunicorn         # WSGI HTTP Server for Python, optional but recommended for production
